from setuptools import setup, find_packages

setup(
    name='punct_package',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
    ],
    entry_points={
        'console_scripts': [
            # Add entry points if you have any command line scripts
        ]
    },
    
)